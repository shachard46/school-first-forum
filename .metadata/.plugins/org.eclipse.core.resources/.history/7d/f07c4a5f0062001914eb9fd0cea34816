<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<!DOCTYPE html>
	<%@ page import="com.shachar.first.*"%>
	<%
		PostDatabase postDatabase = new PostDatabase();
		Post post = postDatabase.createPost(
			request.getSession().getAttribute("currentEmail"), 
			request.getParameter("postText"),
			request.getParameter("email"),
			request.getParameter("compType"),
			request.getParameter("teamNumber"),
			request.getParameter("country"),
			request.getParameter("teamJob"),
			request.getParameter("rookieTime")
		);
		request.getSession().setAttribute("currentpostEmail", post.getEmail());
		request.getSession().setAttribute("validpost", "right");
		response.sendRedirect("forumBase.jsp");
		
%>