<%@ page language="java" contentType="text/html; charset=UTF-8"
	pageEncoding="UTF-8"%>
<!DOCTYPE html>
<%@ page import="com.shachar.first.*"%>
<%
	PostDatabase postDatabase = new PostDatabase();
	UserDatabase userDatabase = new UserDatabase();
	Post post = postDatabase.createPost((String) request.getSession().getAttribute("currentUserEmail"),
			request.getParameter("postText"), Integer.parseInt(request.getParameter("replayFor")),
			request.getParameter("PostSubTopic"));
	userDatabase.UpdatePostNumber(1, (String) request.getSession().getAttribute("currentUserEmail"));
	response.sendRedirect("forumBase.jsp");
%>